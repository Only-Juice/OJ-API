apiVersion: v1
kind: Secret
metadata:
  name: oj-api-secret
  namespace: oj-api
type: Opaque
stringData:
  # 資料庫密碼
  DB_PASSWORD: "ojapi_Password_123"

  # 加密金鑰
  ENCRYPTION_KEY: "pgX4PZom+mX+qiF0sxNLCnWLUXANHx/vMsVARD99DfA="

  # JWT Secret
  JWT_SECRET: "IMKbhmyze3n+vMblITR577b1+TjNIOwusxHalLRoQNc="
  
  # Gitea OAuth 配置（請替換為實際值）
  GITEA_CLIENT_ID: "your-gitea-client-id"
  GITEA_CLIENT_SECRET: "your-gitea-client-secret"
  
  # Email 配置（如果需要的話）
  SMTP_HOST: "smtp.gmail.com"
  SMTP_PORT: "587"
  SMTP_USER: "your-email@gmail.com"
  SMTP_PASSWORD: "your-email-password"
  
  # 其他敏感配置
  # 請根據實際需求添加
