apiVersion: v1
kind: Service
metadata:
  name: oj-sandbox-server
  namespace: oj-api
  labels:
    app: oj-sandbox-server
spec:
  type: ClusterIP
  ports:
  - port: 50051
    targetPort: 50051
    protocol: TCP
    name: grpc
  selector:
    app: oj-sandbox-server
